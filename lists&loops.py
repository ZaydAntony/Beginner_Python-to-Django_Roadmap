# Exercise Build a program that finds the largest and smallest numbers in a list.

numbers = [1,2,8,12,13,14,15,28,56];

for num in numbers:
    print(max(numbers));
    print(min(numbers));
    break;